public enum PropertyState
{
    Available = 0,
    Rented = 1,
    UnderMaintenance = 2
}

public enum DocumentType
{
    DNI = 1,
    Passport = 2,
    License = 3
}

public enum ApprovalState { 
    approved,
    pending,
    rejected
}